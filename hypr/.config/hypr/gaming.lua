-- Gaming window rules, ported from the pre-quattro gaming.conf.
-- Steam, RetroArch, and Moonlight rules already ship as Omarchy defaults.

-- Lutris / Wine games
o.window("lutris", { tag = "-default-opacity", opacity = "1 1" })

-- Minecraft
o.window("Minecraft.*", { tag = "-default-opacity", opacity = "1 1" })
