# Loads /dev/i2c-* so ddcutil can reach the external monitor over DDC/CI.
# Without this the monitor has no kernel backlight device and its brightness
# cannot be changed at all. See the Displays section of the README.
i2c-dev
